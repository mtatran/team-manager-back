import axios, { AxiosInstance } from 'axios'
import { OAuthBearer } from '../../types/index'

export class DriveClient {
  private axios: AxiosInstance

  private getAuthTokenFromCode () {

  }

  setToken (token: OAuthBearer) {

  }

}
